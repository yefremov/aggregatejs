# DISABLED: This workflow has been disabled due to billing/account issues (January 30, 2026)
# The project has migrated to Cirrus CI - see .cirrus.yml for the active CI configuration
# This file is kept as a backup and can be re-enabled once billing issues are resolved

name: CI (DISABLED)

# Triggers disabled - uncomment to re-enable
# on:
#   push:
#     branches: [ master, main ]
#   pull_request:
#     branches: [ master, main ]

jobs:
  test:
    name: Test on Node.js ${{ matrix.node-version }} - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]
        os: [ubuntu-latest]
        include:
          # Also test on macOS and Windows with latest LTS
          - node-version: 20.x
            os: macos-latest
          - node-version: 20.x
            os: windows-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests with coverage
        run: npm run test-cov
      
      - name: Upload coverage to Coveralls
        if: matrix.os == 'ubuntu-latest' && matrix.node-version == '20.x'
        run: |
          npm install coveralls
          npx coveralls < coverage/lcov.info
        env:
          COVERALLS_REPO_TOKEN: ${{ secrets.COVERALLS_REPO_TOKEN }}
